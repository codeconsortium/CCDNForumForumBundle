{% extends 'CCDNForumForumBundle:User:Layout/base.html.twig' %}

{% set LayoutTemplate = ccdn_forum_forum.topic.change_board.layout_template %}

{% block title %}{{- 'title.topic.change_board' | trans({}, 'CCDNForumForumBundle') -}}{% endblock %}


{% block body_content %}

	{%- spaceless -%}

	<section>
		{%- form_theme form ccdn_forum_forum.topic.change_board.form_theme -%}
		<form action="{{- path('ccdn_forum_moderator_topic_change_board_process', {'forumName': forum.name, 'topicId': topic.getId} ) -}}" method="post" {{ form_enctype(form) }} class="form-horizontal">
			<fieldset>

				<div class="control-group form-title">
					<div class="controls">
						<h3>{{- 'title.topic.change_board' | trans({'%topic_title%': topic.getTitle }, 'CCDNForumForumBundle') -}}</h3>
					</div>
				</div>

				{{- form_errors(form) -}}

				<div class="control-group">
					<label class="control-label">
						{{- 'generic.thead.topic' |trans({}, 'CCDNForumForumBundle') -}}
					</label>
					<div class="controls">
						<pre>
							{{- topic.getTitle -}}
						</pre>
					</div>
				</div>

				{{- form_widget(form) -}}

				<div class="form-actions">
					<input type="submit" class="btn btn-danger" value="{{- 'form.button.topic.move' | trans({}, 'CCDNForumForumBundle') -}}" name="submit[post]" id="submit[post]">

					<a class="btn" href="{{- path('ccdn_forum_user_topic_show', {'topicId': topic.id}) -}}">
						{{- 'form.button.cancel' |trans({}, 'CCDNForumForumBundle') -}}
					</a>
				</div>

			</fieldset>
		</form>
	</section>

	{%- endspaceless -%}

{% endblock body_content %}
